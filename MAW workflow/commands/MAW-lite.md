---
description: "Streamlined lightweight workflow for non-trivial tasks: brainstorm -> single planner -> single worker -> final report."
---

Invoke the `maw-lite` skill to orchestrate this objective:

$ARGUMENTS
